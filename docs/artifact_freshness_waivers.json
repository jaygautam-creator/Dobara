[
  {
    "artifact": "artifacts/summary.json",
    "commit": "afe326fc393ae62c5f85656768b2ce267aa6506f",
    "reason": "afe326f's only eval/ change is eval/sensitivity.py's new search_break_even_vs_razorpay_default (plus its private helpers _point_diff_vs_razorpay_default, _revocation_ratio_at, ExtendedSearchResult) -- additive functions, no edit to any existing function. Verified via `git show afe326f -- eval/sensitivity.py`: the diff is a pure addition (+265/-0 lines) appended after sweep_other_axes, and no other eval/, agent/, models/, or sim/ file changed in this commit. eval/run.py (summary.json's generator) does not import or call the new function."
  },
  {
    "artifact": "artifacts/demo_batch.json",
    "commit": "afe326fc393ae62c5f85656768b2ce267aa6506f",
    "reason": "Same as artifacts/summary.json: afe326f's only eval/ change is the new, additive search_break_even_vs_razorpay_default in eval/sensitivity.py. scripts/build_demo_fixture.py does not import eval/sensitivity.py at all."
  },
  {
    "artifact": "artifacts/money_chart_data.json",
    "commit": "afe326fc393ae62c5f85656768b2ce267aa6506f",
    "reason": "Same as artifacts/summary.json: afe326f's only eval/ change is the new, additive search_break_even_vs_razorpay_default in eval/sensitivity.py. scripts/build_money_chart.py does not import it."
  },
  {
    "artifact": "artifacts/home_demo.json",
    "commit": "afe326fc393ae62c5f85656768b2ce267aa6506f",
    "reason": "Same as artifacts/summary.json: afe326f's only eval/ change is the new, additive search_break_even_vs_razorpay_default in eval/sensitivity.py. scripts/build_home_demo.py goes through api/demo.py's live decide() path and does not import eval/sensitivity.py."
  },
  {
    "artifact": "artifacts/compliance_rules.json",
    "commit": "afe326fc393ae62c5f85656768b2ce267aa6506f",
    "reason": "afe326f made no change to agent/compliance.py or scripts/build_compliance_rules.py -- its only agent//models//eval//sim/ change is the additive search_break_even_vs_razorpay_default in eval/sensitivity.py, which the compliance-rules exporter does not import."
  },
  {
    "artifact": "artifacts/llm_cache/ask_why.json",
    "commit": "afe326fc393ae62c5f85656768b2ce267aa6506f",
    "reason": "afe326f's only watched-path change is the additive search_break_even_vs_razorpay_default in eval/sensitivity.py; llm/ and scripts/generate_ask_why.py are untouched, and the cache narrates artifacts/demo_batch.json's decisions, which this commit does not affect (see that artifact's own waiver above)."
  },
  {
    "artifact": "artifacts/summary.json",
    "commit": "bfa52dc24c55727f54b023671114e8016b45b5a4",
    "reason": "bfa52dc added eval/provenance.py::content_hash(), a new pure function. It is called only by scripts/check_artifact_freshness.py::check_hash_dependency() and by scripts/generate_ask_why.py::save_cache() (to stamp demo_batch_content_hash). Nothing in eval/run.py or its call graph invokes it, so artifacts/summary.json's generation is unaffected. Verified via `grep -rn content_hash eval/ scripts/` before recording this waiver."
  },
  {
    "artifact": "artifacts/sensitivity.json",
    "commit": "bfa52dc24c55727f54b023671114e8016b45b5a4",
    "reason": "Same as artifacts/summary.json: bfa52dc's only eval/ change is the new, unused-by-generators content_hash() helper. eval/sensitivity.py does not import it."
  },
  {
    "artifact": "artifacts/demo_batch.json",
    "commit": "bfa52dc24c55727f54b023671114e8016b45b5a4",
    "reason": "Same as artifacts/summary.json: bfa52dc's only eval/ change is the new, unused-by-generators content_hash() helper. scripts/build_demo_fixture.py does not import it."
  },
  {
    "artifact": "artifacts/money_chart_data.json",
    "commit": "bfa52dc24c55727f54b023671114e8016b45b5a4",
    "reason": "Same as artifacts/summary.json: bfa52dc's only eval/ change is the new, unused-by-generators content_hash() helper. scripts/build_money_chart.py does not import it."
  },
  {
    "artifact": "artifacts/llm_cache/ask_why.json",
    "commit": "bfa52dc24c55727f54b023671114e8016b45b5a4",
    "reason": "bfa52dc's eval/ change (content_hash() addition) is not imported by llm/ or scripts/generate_ask_why.py's narration path -- generate_ask_why.py calls content_hash() only when *writing* the cache (to stamp demo_batch_content_hash), not as part of narrating a decision, so the narrated text is unaffected. content_hash()'s own behavior for this artifact's actual dependency is separately covered by the HASH_DEPENDENCIES content check below, which passes."
  },
  {
    "artifact": "artifacts/summary.json",
    "commit": "ad1e671e1d445fd230add703dced78974c96264a",
    "reason": "ad1e671's only change under a watched path is eval/runner.py's opt-in per-beat trace: a new AttemptEvent dataclass, a MandateResult.events list defaulting to empty, and a `trace: bool = False` parameter threaded through run_arm/_run_cadence_arm/_run_dobara_arm/_draw_attempt. Every append is guarded by `if trace`, no draw, ordering or scored field is touched, and the only caller that passes trace=True is the new scripts/build_home_demo.py. Proven, not just argued: tests/test_runner_trace.py runs aggressive_8x and dobara with and without the trace over a shared world and asserts the two result lists are equal after clearing `events` -- so any field this artifact reports would have to differ for that test to pass. eval/run.py calls run_arm without `trace`, taking the default."
  },
  {
    "artifact": "artifacts/sensitivity.json",
    "commit": "ad1e671e1d445fd230add703dced78974c96264a",
    "reason": "ad1e671's only change under a watched path is eval/runner.py's opt-in per-beat trace: a new AttemptEvent dataclass, a MandateResult.events list defaulting to empty, and a `trace: bool = False` parameter threaded through run_arm/_run_cadence_arm/_run_dobara_arm/_draw_attempt. Every append is guarded by `if trace`, no draw, ordering or scored field is touched, and the only caller that passes trace=True is the new scripts/build_home_demo.py. Proven, not just argued: tests/test_runner_trace.py runs aggressive_8x and dobara with and without the trace over a shared world and asserts the two result lists are equal after clearing `events` -- so any field this artifact reports would have to differ for that test to pass. eval/sensitivity.py calls run_arm without `trace`, taking the default."
  },
  {
    "artifact": "artifacts/demo_batch.json",
    "commit": "ad1e671e1d445fd230add703dced78974c96264a",
    "reason": "ad1e671's only change under a watched path is eval/runner.py's opt-in per-beat trace: a new AttemptEvent dataclass, a MandateResult.events list defaulting to empty, and a `trace: bool = False` parameter threaded through run_arm/_run_cadence_arm/_run_dobara_arm/_draw_attempt. Every append is guarded by `if trace`, no draw, ordering or scored field is touched, and the only caller that passes trace=True is the new scripts/build_home_demo.py. Proven, not just argued: tests/test_runner_trace.py runs aggressive_8x and dobara with and without the trace over a shared world and asserts the two result lists are equal after clearing `events` -- so any field this artifact reports would have to differ for that test to pass. scripts/build_demo_fixture.py goes through api/demo.py's live decide() path and never calls run_arm at all."
  },
  {
    "artifact": "artifacts/money_chart_data.json",
    "commit": "ad1e671e1d445fd230add703dced78974c96264a",
    "reason": "ad1e671's only change under a watched path is eval/runner.py's opt-in per-beat trace: a new AttemptEvent dataclass, a MandateResult.events list defaulting to empty, and a `trace: bool = False` parameter threaded through run_arm/_run_cadence_arm/_run_dobara_arm/_draw_attempt. Every append is guarded by `if trace`, no draw, ordering or scored field is touched, and the only caller that passes trace=True is the new scripts/build_home_demo.py. Proven, not just argued: tests/test_runner_trace.py runs aggressive_8x and dobara with and without the trace over a shared world and asserts the two result lists are equal after clearing `events` -- so any field this artifact reports would have to differ for that test to pass. scripts/build_money_chart.py calls run_arm without `trace`, taking the default."
  },
  {
    "artifact": "artifacts/llm_cache/ask_why.json",
    "commit": "ad1e671e1d445fd230add703dced78974c96264a",
    "reason": "ad1e671's only change under a watched path is eval/runner.py's opt-in per-beat trace: a new AttemptEvent dataclass, a MandateResult.events list defaulting to empty, and a `trace: bool = False` parameter threaded through run_arm/_run_cadence_arm/_run_dobara_arm/_draw_attempt. Every append is guarded by `if trace`, no draw, ordering or scored field is touched, and the only caller that passes trace=True is the new scripts/build_home_demo.py. Proven, not just argued: tests/test_runner_trace.py runs aggressive_8x and dobara with and without the trace over a shared world and asserts the two result lists are equal after clearing `events` -- so any field this artifact reports would have to differ for that test to pass. The cache narrates artifacts/demo_batch.json's decisions, which are unchanged by this commit (the HASH_DEPENDENCIES content check confirms it independently); llm/ and scripts/generate_ask_why.py are untouched here."
  },
  {
    "artifact": "artifacts/summary.json",
    "commit": "6a20367e6e1bac0b67b5c2de60fd03bd2bb71a4b",
    "reason": "6a20367's only watched-path change is agent/decide.py: a pure docstring addition (+7/-0 lines) to `_tie_break_score` pointing readers at docs/DECISIONS.md for the calibrator bake-off follow-up findings. Verified via `git show 6a20367 -- agent/decide.py`: no code, control flow, or default value changed. eval/run.py's call graph is unaffected."
  },
  {
    "artifact": "artifacts/sensitivity.json",
    "commit": "6a20367e6e1bac0b67b5c2de60fd03bd2bb71a4b",
    "reason": "Same as artifacts/summary.json: 6a20367's only watched-path change is a pure docstring addition to agent/decide.py::_tie_break_score. eval/sensitivity.py's call graph is unaffected."
  },
  {
    "artifact": "artifacts/demo_batch.json",
    "commit": "6a20367e6e1bac0b67b5c2de60fd03bd2bb71a4b",
    "reason": "Same as artifacts/summary.json: 6a20367's only watched-path change is a pure docstring addition to agent/decide.py::_tie_break_score. scripts/build_demo_fixture.py's live decide() path is unaffected."
  },
  {
    "artifact": "artifacts/money_chart_data.json",
    "commit": "6a20367e6e1bac0b67b5c2de60fd03bd2bb71a4b",
    "reason": "Same as artifacts/summary.json: 6a20367's only watched-path change is a pure docstring addition to agent/decide.py::_tie_break_score. scripts/build_money_chart.py's call graph is unaffected."
  },
  {
    "artifact": "artifacts/home_demo.json",
    "commit": "6a20367e6e1bac0b67b5c2de60fd03bd2bb71a4b",
    "reason": "Same as artifacts/summary.json: 6a20367's only watched-path change is a pure docstring addition to agent/decide.py::_tie_break_score. scripts/build_home_demo.py's live decide() path is unaffected."
  },
  {
    "artifact": "artifacts/compliance_rules.json",
    "commit": "6a20367e6e1bac0b67b5c2de60fd03bd2bb71a4b",
    "reason": "6a20367 made no change to agent/compliance.py or scripts/build_compliance_rules.py -- its only watched-path change is a pure docstring addition to agent/decide.py::_tie_break_score, which the compliance-rules exporter does not import."
  },
  {
    "artifact": "artifacts/llm_cache/ask_why.json",
    "commit": "6a20367e6e1bac0b67b5c2de60fd03bd2bb71a4b",
    "reason": "6a20367's only watched-path change is a pure docstring addition to agent/decide.py::_tie_break_score; llm/ and scripts/generate_ask_why.py are untouched, and the cache narrates artifacts/demo_batch.json's decisions, which this commit does not affect (see that artifact's own waiver above)."
  },
  {
    "artifact": "artifacts/summary.json",
    "commit": "684caf2c7ad321b3cebd9118830a54e610e9fe6a",
    "reason": "684caf2's only change is a prose (note-text) fix inside eval/sensitivity.py::search_break_even_vs_razorpay_default. eval/run.py (artifacts/summary.json's generator) does not import eval.sensitivity at all -- verified directly, no import statement referencing it anywhere in eval/run.py. Unlike the other artifacts this session touched, this one is NOT regenerated to clear this waiver: the owner's explicit instruction for the isotonic revert was 'do NOT rerun make eval', so a waiver -- not a real rerun -- is the correct mechanism here, same as the original 6a20367 commit's own docstring-only waivers above."
  },
  {
    "artifact": "artifacts/sensitivity.json",
    "commit": "684caf2c7ad321b3cebd9118830a54e610e9fe6a",
    "reason": "684caf2 touches sensitivity.json's own generator, but only the prose branch inside search_break_even_vs_razorpay_default that fires when the losing arm changes -- re-verified against this repo's current (isotonic, dobara-always-wins) state: unaffected, still produces the same 'dobara beats the other arm' text and the same found/break_even_value/bracket numbers as before the fix. Not regenerated to clear this waiver: the owner's explicit instruction for the isotonic revert was 'do NOT rerun make eval' (python -m eval.sensitivity is part of that same rerun), so a waiver is the correct mechanism, not a live rerun."
  }
]

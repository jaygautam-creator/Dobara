// The two links a reviewer is most likely to want and least likely to hunt for: the
// 5-minute pitch and the source. Defined once here because they appear in the
// persistent header, the mobile menu, the footer, and the `/` hero -- four places that
// must never disagree about where the video lives.
export const PITCH_VIDEO_URL = "https://youtu.be/e3LlVMtq5LI";
export const REPO_URL = "https://github.com/jaygautam-creator/Dobara";
